//Main.cpp til test af LINK laget

#include <iostream>
#include "include/Link.h"
#include <string>

int main()
{
	string str = "hej derude."
	Link link(length(str));
	
	link.send(str, length(str));
	
	
	return 0;
}
